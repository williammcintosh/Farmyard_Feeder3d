using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

/****************************************
    DEPRECIATED!!!!
****************************************/
/*
public class AnimalClassScript
{
    public string animalName;
    public int foodCount;
    public string dateLastPlay;
    public float metersTraveled;
    public bool equipped;
    public AnimalClassScript(string newName, string newScore, string newDate, string newMeters, string newEquipped)
    {
        animalName = String.Copy(newName);
        foodCount = int.Parse(newScore);
        dateLastPlay = String.Copy(newDate);
        metersTraveled = float.Parse(newMeters);
        equipped = bool.Parse(newEquipped);
    }
    public AnimalClassScript(AnimalClassScript copyLeader)
    {
        animalName = String.Copy(copyLeader.animalName);
        foodCount = copyLeader.foodCount;
        dateLastPlay = String.Copy(copyLeader.dateLastPlay);
        metersTraveled = copyLeader.metersTraveled;
        equipped = copyLeader.equipped;
    }
}
*/
